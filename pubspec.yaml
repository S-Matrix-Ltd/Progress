name: progress
description: Monthly Duty & OT Statement
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  crypto: ^3.0.3
  pdf: ^3.10.7
  printing: ^5.12.0
  url_launcher: ^6.2.4
  flutter_local_notifications: ^17.2.3
  timezone: ^0.9.4
  http: ^1.2.0
  local_auth: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
